import { AppRegistry } from 'react-native';
import App from './App'; // Assuming this is your main component

// Register the main component of your application
AppRegistry.registerComponent('final-project', () => App);